name: Schedule development validation

on:
  push:
    branches: [feat/durable-schedules]

permissions:
  contents: write

concurrency:
  group: schedules-development
  cancel-in-progress: false

jobs:
  validate:
    if: github.ref == 'refs/heads/feat/durable-schedules'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: mq
          POSTGRES_PASSWORD: mq_test_only
          POSTGRES_DB: mq_test
        ports: ['5432:5432']
        options: --health-cmd "pg_isready -U mq -d mq_test" --health-interval 5s --health-timeout 5s --health-retries 10
    env:
      MQ_TEST_DATABASE_URL: postgresql://mq:mq_test_only@localhost:5432/mq_test
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.4.2'
      - name: Complete regression fixtures and format authored sources
        run: |
          node .github/schedule-qualification.mjs
          bun install --frozen-lockfile
          bun run format
          bun run check:tooling
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -u
          if ! git diff --cached --quiet; then
            git commit -m 'test: qualify real schedule occurrences and format the implementation'
            git push origin HEAD:feat/durable-schedules
          fi
      - name: Verify full quality gate with installed PostgreSQL consumers
        run: bun run check
