name: SQLite resource verification
on:
  push:
    branches: [feat/sqlite-resources]
permissions:
  contents: read
concurrency:
  group: sqlite-resource-development
  cancel-in-progress: true
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.4.2'
      - run: bun install --frozen-lockfile
      - run: bun run typecheck && bun run typecheck:minimum
      - name: Verify native event resources
        run: bun test tests/unit/sqlite-resources-api.test.ts tests/integration/sqlite-events.test.ts
      - name: Verify lint and formatting
        if: always()
        run: |
          bun run lint
          bun run format
          git diff --exit-code
          bun run check:tooling
